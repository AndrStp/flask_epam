# from app import db


# association_table = db.Table('student-course',
#     db.Column('course_id', db.Integer, db.ForeignKey('course.id'), primary_key=True),
#     db.Column('student_id', db.Integer, db.ForeignKey('student.id'), primary_key=True)
# )